/*
Where Statement 
*, <>, <, >, And, Or, Like, Null, Not Null, In

Different database
[Name of the database].dbo.[table name]

% = wildcards
*/

SELECT *
FROM EmployeeDemographics
WHERE FirstName IN ('Jim', 'Michael') 